import React from 'react';

import {Text, View} from 'react-native';

const AboutUsScreen = ({}) => {
    return (
        <Text>
            AboutUsScreen
        </Text>
    );
};

export default AboutUsScreen;
