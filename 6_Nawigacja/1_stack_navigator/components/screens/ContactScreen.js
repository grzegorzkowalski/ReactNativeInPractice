import React from 'react';

import {Text, View} from 'react-native';

const ContactScreen = () => {
    return (
        <Text>
            ContactScreen
        </Text>
    );
};

export default ContactScreen;
