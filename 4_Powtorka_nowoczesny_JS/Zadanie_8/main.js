const employee = {
	name: "Krzysztof",
	surname: "Nowak",
	age: 54,
	occupation: "Policjant"
}

const {name, surname, age, occupation} = employee;

console.log(`${name} ${surname} lat: ${age}. Wykonywany zawód: ${occupation}`);
