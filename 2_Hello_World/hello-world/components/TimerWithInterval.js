import React from 'react';
import {Text} from 'react-native';

const TimerWithInterval = ({time}) => {
    return (
        <Text>
            {time}
        </Text>
    );
};

export default TimerWithInterval;
